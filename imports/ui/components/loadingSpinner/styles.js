const styles = theme => ({
  loadingText: {
    textAlign: "center",
    color: "white"
  },
  loadingContainer: {
    display: "flex",
    alignContent: "center"
  },
  progress: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center"
  }
});

export default styles;
