const styles = theme => ({
  loadingText: {
    textAlign: "center"
  },
  loadingContainer: {
    display: "flex",
    alignContent: "center",
    color: "white"
  },
  progress: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center"
  }
});

export default styles;
